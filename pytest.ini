[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session

pythonpath = .

markers =
    open_notebook: marks tests that require a live Open Notebook connection (auto-skipped when not running)
